// PostForm.jsx
import React from 'react';

const PostForm = ({ initialData, onSubmit }) => {
  return (
    <form>
      {/* TODO: Form fields for title, content, categories, tags, image */}
    </form>
  );
};

export default PostForm;
